name: Run linter

on: push

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependence
        run: |
          pip install poetry
      - name: Run linter
        run: |
          make lint
